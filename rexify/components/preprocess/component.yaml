name: Preprocess

inputs:
  - {name: test_size, type: Float, default: "0.3"}

outputs:
  - {name: output_dir, type: String}

implementation:
  container:
    image: joseprsm/rexify-preprocess
    command: [python, preprocess.py]
    args: [--output-dir, {outputPath: output_dir}]
name: Preprocess
description: ""

inputs:
  - {name: event_dir, type: String}
  - {name: test_size, type: Float}

outputs:
  - {name: output_dir, type: String}

implementation:
  container:
    image: joseprsm/rexify-preprocess
    command: [python, preprocess.py]
    args: [
      --input-dir, {inputPath: input_dir},
      --output-dir, {outputPath: output_dir}
    ]